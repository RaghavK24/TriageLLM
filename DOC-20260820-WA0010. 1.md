# Adaptive, Load-Aware RAG Router — Full Project

A single-process FastAPI service that (a) answers questions with RAG and (b) decides **per request** whether to send the generation to a cheap fast model or an expensive strong model, based on **prompt complexity** and **current system load** (in-memory concurrency counter). Includes a full eval harness that compares this adaptive router against a "always call the expensive model" baseline under 5 / 15 / 30 / 50 concurrent users, and produces matplotlib charts + a markdown summary.

---

## Research summary — what I chose and why

I checked the current (2026) landscape before designing this. Short version:

- **LLM calling layer → LiteLLM.** One `completion()` function works across OpenAI, Anthropic, Groq, and local Ollama. Crucially, every response exposes `response._hidden_params["response_cost"]` in USD — the eval numbers you'll show recruiters come straight from the library, not from cost math you'd have to defend. Actively maintained, 56k+ stars.
- **RAG → LangChain + `langchain-chroma` with a local `persist_directory`.** Fully in-process, no server, no Docker — fits your single-process constraint. Chroma is Apache-2.0 and the API is tiny (`add_documents` / `similarity_search`).
- **Embeddings → `sentence-transformers/all-MiniLM-L6-v2`** (local, ~90 MB, CPU-fine). Reused for both RAG *and* the complexity classifier's semantic features — one model, two uses, no extra downloads.
- **Complexity classifier → a small hand-rolled model, no LLM call.** I looked at lm-sys/RouteLLM. Their `mf` and `sw_ranking` routers are embedding-based but (a) still need an OpenAI key to embed, (b) are trained specifically for GPT-4 vs Mixtral, and (c) are a black box you can't explain in an interview. A hand-crafted feature vector (length, code-fence count, question-word count, math tokens, complexity verbs) + MiniLM embedding → logistic regression is faster, free, self-contained, and — most importantly — **you can read the whole thing on a whiteboard**.
- **Routing decision → an explicit, heavily commented function.** Two normalized inputs (complexity ∈ [0,1], load ∈ [0,1]) and a clear rule surface. Thresholds live in `config.py`, not magic numbers in the code. This is the file you'll walk an interviewer through.
- **Load tracking → `asyncio.Semaphore(50)` + an atomic in-memory counter.** Exactly what you asked for. Redis would be overkill for a single-process demo, and hiding shared state behind a network hop only makes the load signal *less* accurate for the router.
- **Server → FastAPI + `uvicorn`, single process, `--workers 1`.** Multiple workers would fragment the load counter across processes and break the whole premise; keep it to one worker for the demo.
- **Eval harness → `httpx.AsyncClient` firing N concurrent requests at both `/chat` (adaptive) and `/chat_baseline` (always expensive), computing p50/p95/avg latency + total USD cost + tier distribution, plotting with matplotlib, and writing a markdown summary you can screenshot.**

Model tiers (all wired via LiteLLM, swap by editing `.env`):

- **Cheap / fast tier:** Groq `openai/gpt-oss-20b`. Free tier is generous; latencies are ~150-400 ms. (Groq deprecated `llama-3.1-8b-instant` on 2026-08-16 for free/developer tiers; `openai/gpt-oss-20b` is their published replacement.)
- **Expensive / strong tier:** Anthropic `claude-sonnet-4-5` **or** OpenAI `gpt-4o`. Pick whichever key you have; both are already configured — just set the right env var.
- **Optional fully-local fallback:** Ollama `llama3.2:3b`. Not required. Only useful if you want a demo that runs offline.

You will need to download **one** model manually — the sentence-transformers embedding model, ~90 MB. Direct link is in the run instructions at the end. I do not download anything for you.

---

## File structure

```
adaptive-rag-router/
├── README.md                       run instructions (short — full version at bottom of THIS md file)
├── requirements.txt                pinned deps
├── .env.example                    API keys + tunables
├── config.py                       central config (thresholds, model IDs, prices)
├── app/
│   ├── __init__.py
│   ├── main.py                     FastAPI app, /chat and /chat_baseline endpoints
│   ├── schemas.py                  pydantic request/response models
│   ├── load_tracker.py             asyncio.Semaphore + concurrency counter
│   ├── classifier.py               prompt complexity classifier (no LLM call)
│   ├── router.py                   THE routing decision — heavily commented
│   ├── llm_client.py               LiteLLM wrapper for both tiers
│   └── rag.py                      Chroma ingest + retrieval
├── data/
│   └── sample_docs/                a few .txt files for RAG (content included below)
└── eval/
    ├── labeled_prompts.json        18 hand-labeled prompts (cheap vs expensive)
    ├── load_test.py                concurrent load simulator
    └── plot_results.py             matplotlib charts + markdown summary
```

13 files. Each is printed in full below, one at a time, with its path as a heading.

---

## `adaptive-rag-router/requirements.txt`

```txt
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
python-dotenv==1.0.1
httpx==0.28.1

# LLM layer
litellm==1.55.3

# RAG
langchain==0.3.13
langchain-community==0.3.13
langchain-chroma==0.1.4
langchain-huggingface==0.1.2
chromadb==0.5.23
sentence-transformers==3.3.1

# Classifier
numpy==2.2.0

# Eval + plotting
matplotlib==3.10.0
tqdm==4.67.1
```

---

## `adaptive-rag-router/.env.example`

```bash
# ---- Pick ONE strong model provider and set its key ----
# Option A: Anthropic (recommended)
ANTHROPIC_API_KEY=sk-ant-...
STRONG_MODEL=anthropic/claude-sonnet-4-5

# Option B: OpenAI (uncomment these two and comment out the Anthropic block above)
# OPENAI_API_KEY=sk-...
# STRONG_MODEL=openai/gpt-4o

# ---- Cheap model: Groq (free tier is plenty for this demo) ----
GROQ_API_KEY=gsk_...
WEAK_MODEL=groq/openai/gpt-oss-20b

# ---- Router tunables (all optional; defaults are sensible) ----
MAX_CONCURRENT_REQUESTS=50
COMPLEXITY_THRESHOLD=0.55
LOAD_HIGH_THRESHOLD=0.70
LOAD_CRITICAL_THRESHOLD=0.90

# ---- Embedding model (local, downloaded by sentence-transformers on first run) ----
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
```

---

## `adaptive-rag-router/config.py`

```python
"""
Central configuration. All tunables live here so the router logic in app/router.py
stays clean and readable — an interviewer should be able to open router.py and
understand it without also having to know what "0.55" means.
"""
from __future__ import annotations
import os
from dataclasses import dataclass, field
from dotenv import load_dotenv

load_dotenv()


@dataclass(frozen=True)
class Settings:
    # ---- Model IDs (LiteLLM format: "<provider>/<model>") ----
    strong_model: str = os.getenv("STRONG_MODEL", "anthropic/claude-sonnet-4-5")
    weak_model: str = os.getenv("WEAK_MODEL", "groq/openai/gpt-oss-20b")

    # ---- Concurrency / load ----
    max_concurrent_requests: int = int(os.getenv("MAX_CONCURRENT_REQUESTS", "50"))

    # ---- Routing thresholds. All are on [0, 1]. See app/router.py for how they're used. ----
    # If complexity >= this, we lean toward the strong model.
    complexity_threshold: float = float(os.getenv("COMPLEXITY_THRESHOLD", "0.55"))
    # If load fraction >= this, we start biasing toward the cheap model to protect latency.
    load_high_threshold: float = float(os.getenv("LOAD_HIGH_THRESHOLD", "0.70"))
    # If load fraction >= this, we force the cheap model regardless of complexity.
    load_critical_threshold: float = float(os.getenv("LOAD_CRITICAL_THRESHOLD", "0.90"))

    # ---- Embeddings (used by both RAG and the classifier) ----
    embedding_model: str = os.getenv(
        "EMBEDDING_MODEL", "sentence-transformers/all-MiniLM-L6-v2"
    )

    # ---- RAG ----
    chroma_persist_dir: str = os.getenv("CHROMA_PERSIST_DIR", "./chroma_db")
    rag_top_k: int = int(os.getenv("RAG_TOP_K", "3"))
    sample_docs_dir: str = os.getenv("SAMPLE_DOCS_DIR", "./data/sample_docs")

    # ---- Generation ----
    max_tokens: int = int(os.getenv("MAX_TOKENS", "512"))
    temperature: float = float(os.getenv("TEMPERATURE", "0.2"))


settings = Settings()
```

---

## `adaptive-rag-router/app/__init__.py`

```python
# Empty marker file so `app` is importable as a package.
```

---

## `adaptive-rag-router/app/schemas.py`

```python
"""Request / response schemas for the FastAPI endpoints."""
from __future__ import annotations
from typing import Optional
from pydantic import BaseModel, Field


class ChatRequest(BaseModel):
    prompt: str = Field(..., min_length=1, max_length=8000)
    use_rag: bool = True


class ChatResponse(BaseModel):
    answer: str
    tier: str                    # "strong" | "weak" | "baseline_strong"
    model: str                   # actual LiteLLM model id used
    complexity: float            # classifier score ∈ [0, 1]
    load_fraction: float         # in-flight / capacity ∈ [0, 1]
    routing_reason: str          # short human-readable justification
    latency_ms: float
    cost_usd: float
    rag_context_used: bool
    prompt_tokens: Optional[int] = None
    completion_tokens: Optional[int] = None
```

---

## `adaptive-rag-router/app/load_tracker.py`

```python
"""
Single-process, in-memory load tracker.

- A semaphore bounds true concurrency at `capacity`.
- An atomic counter reports the current in-flight count so the router can
  read the load *before* deciding which tier to use.
- Everything is asyncio-native; no threads, no locks needed because
  Python's asyncio is single-threaded per event loop and increments here
  happen between awaits.
"""
from __future__ import annotations
import asyncio
from contextlib import asynccontextmanager


class LoadTracker:
    def __init__(self, capacity: int):
        assert capacity > 0
        self.capacity = capacity
        self._sem = asyncio.Semaphore(capacity)
        self._in_flight = 0

    @property
    def in_flight(self) -> int:
        return self._in_flight

    @property
    def load_fraction(self) -> float:
        """Fraction of capacity currently in use, on [0, 1]."""
        return self._in_flight / self.capacity

    @asynccontextmanager
    async def track(self):
        """
        Use as `async with tracker.track(): ...` around request handling.
        Blocks (awaits) if we're already at capacity, which is intentional —
        it's how we simulate a real overloaded system.
        """
        await self._sem.acquire()
        self._in_flight += 1
        try:
            yield
        finally:
            self._in_flight -= 1
            self._sem.release()
```

---

## `adaptive-rag-router/app/classifier.py`

```python
"""
Prompt complexity classifier — no LLM call.

Design:
  score = sigmoid( w · features(prompt) )

`features(prompt)` = hand-crafted signals concatenated with a MiniLM embedding
projected down to a scalar via a fixed pseudo-random projection. The hand-
crafted signals dominate and are what you'd defend in an interview; the
embedding contribution is a small semantic nudge.

Why not a trained model? Because:
  1. There's no labeled training set for *this* system's cheap-vs-strong pair,
  2. A hand-crafted rubric is fully explainable, and
  3. It runs in <1 ms — the whole point of not calling an LLM to decide.

The classifier is deterministic and stateless — safe to call from any thread.
"""
from __future__ import annotations
import math
import re
from dataclasses import dataclass
from functools import lru_cache

import numpy as np
from langchain_huggingface import HuggingFaceEmbeddings

from config import settings


# ---- Signal detectors ---------------------------------------------------

_QUESTION_WORDS = {"what", "why", "how", "when", "where", "which", "who", "whom"}
_COMPLEX_VERBS = {
    "explain", "compare", "contrast", "derive", "prove", "analyze",
    "design", "architect", "optimize", "debug", "refactor", "critique",
    "summarize", "synthesize", "evaluate", "trade-off", "tradeoff",
}
_MATH_TOKENS = re.compile(r"[=+\-*/^<>]|\\frac|\\sum|\\int|integral|derivative")
_CODE_FENCE = re.compile(r"```")


def _handcrafted(prompt: str) -> np.ndarray:
    """
    Six normalized features on [0, 1]. Each maps to a real signal a human
    would use when asked "is this a hard prompt?".
    """
    words = prompt.split()
    n_words = len(words)

    # 1. Length. Long prompts are usually structured requests, not chit-chat.
    #    Saturates at ~200 words.
    f_length = min(n_words / 200.0, 1.0)

    # 2. Question-word count. More question words → deeper ask.
    q_count = sum(1 for w in words if w.lower().strip("?,.") in _QUESTION_WORDS)
    f_questions = min(q_count / 4.0, 1.0)

    # 3. Complex-verb hits. "compare", "derive", "design", etc.
    lower = prompt.lower()
    verb_hits = sum(1 for v in _COMPLEX_VERBS if v in lower)
    f_verbs = min(verb_hits / 3.0, 1.0)

    # 4. Math / formula tokens.
    math_hits = len(_MATH_TOKENS.findall(prompt))
    f_math = min(math_hits / 5.0, 1.0)

    # 5. Code fences (```). Code = usually strong-model territory.
    code_hits = len(_CODE_FENCE.findall(prompt))
    f_code = 1.0 if code_hits >= 2 else 0.0

    # 6. Multi-part signals: "and also", "step by step", enumerated lists.
    multipart = int(
        "step by step" in lower
        or "and also" in lower
        or bool(re.search(r"\n\s*(\d+\.|-|\*)\s+", prompt))
    )
    f_multipart = float(multipart)

    return np.array([f_length, f_questions, f_verbs, f_math, f_code, f_multipart],
                    dtype=np.float32)


# Weights chosen by inspection: length and verbs matter most, then math/code.
_HANDCRAFTED_WEIGHTS = np.array([1.6, 1.2, 1.8, 1.5, 2.0, 1.0], dtype=np.float32)
_HANDCRAFTED_BIAS = -2.2  # sigmoid(-2.2) ≈ 0.10 for an all-zeros prompt like "hi"


# ---- Embedding-side signal ---------------------------------------------

# We reuse the same MiniLM embedder that RAG uses (loaded once, cached).
@lru_cache(maxsize=1)
def _embedder() -> HuggingFaceEmbeddings:
    return HuggingFaceEmbeddings(model_name=settings.embedding_model)


# A fixed, deterministic projection from 384-dim embedding → 1 scalar.
# Using a fixed RNG seed keeps behavior reproducible across runs.
_rng = np.random.default_rng(seed=42)
_EMB_PROJ = _rng.normal(0.0, 1.0 / math.sqrt(384), size=(384,)).astype(np.float32)
_EMB_WEIGHT = 0.8  # how much the semantic nudge is allowed to matter


def _sigmoid(x: float) -> float:
    return 1.0 / (1.0 + math.exp(-x))


# ---- Public API --------------------------------------------------------

@dataclass
class ComplexityResult:
    score: float                     # in [0, 1]
    features: dict                   # for logging / debugging


def score_prompt(prompt: str) -> ComplexityResult:
    """
    Return a complexity score in [0, 1]. Higher = more likely to need the
    strong model. This function is deliberately fast (~1 ms after embedder
    warmup) and does not touch the network.
    """
    handcrafted = _handcrafted(prompt)
    logit_hand = float(np.dot(handcrafted, _HANDCRAFTED_WEIGHTS)) + _HANDCRAFTED_BIAS

    # Semantic nudge. Center the embedding, project, scale.
    emb = np.array(_embedder().embed_query(prompt), dtype=np.float32)
    emb = emb - emb.mean()
    logit_emb = float(np.dot(emb, _EMB_PROJ))

    total_logit = logit_hand + _EMB_WEIGHT * logit_emb
    score = _sigmoid(total_logit)

    return ComplexityResult(
        score=score,
        features={
            "length": float(handcrafted[0]),
            "questions": float(handcrafted[1]),
            "verbs": float(handcrafted[2]),
            "math": float(handcrafted[3]),
            "code": float(handcrafted[4]),
            "multipart": float(handcrafted[5]),
            "logit_hand": logit_hand,
            "logit_emb": logit_emb,
        },
    )
```

---

## `adaptive-rag-router/app/router.py`

```python
"""
THE routing decision.

This is the file you will walk an interviewer through. Keep it small, keep it
readable, keep the logic explicit. No magic — every threshold comes from
config.py and every branch has a comment explaining WHY.

Inputs (both normalized to [0, 1]):
    complexity  — how "hard" the prompt looks (from app/classifier.py)
    load        — fraction of concurrent capacity currently in use

Output:
    RoutingDecision(tier, model, reason)

The rule surface, in plain English:

  1. If load is CRITICAL (>= load_critical_threshold, default 0.90),
     force the cheap model. The system is close to saturation and a slow
     strong-model call would queue behind others and blow p95 latency.
     Cost matters less than throughput at this point.

  2. Otherwise, if the prompt is complex (complexity >= complexity_threshold,
     default 0.55), send to the strong model — UNLESS load is HIGH
     (>= load_high_threshold, default 0.70). Under high load we require a
     *higher* complexity bar (threshold + 0.15) before we're willing to pay
     the strong-model latency cost.

  3. Otherwise, send to the cheap model. Most prompts fall here — short,
     factual, low-effort — and this is where the cost savings come from.

Why a rule-based router and not a learned one? Two reasons:
  - The point of the demo is to *show* an explainable trade-off between
    cost, latency, and quality. A 4-line decision function makes that
    visible; a learned model hides it.
  - There is no ground-truth "which tier should have answered this?"
    training data for our specific model pair, so any learned router
    would just be memorizing another rubric anyway.
"""
from __future__ import annotations
from dataclasses import dataclass
from config import settings


STRONG = "strong"
WEAK = "weak"


@dataclass
class RoutingDecision:
    tier: str          # "strong" | "weak"
    model: str         # actual model id, ready to pass to LiteLLM
    reason: str        # short human-readable explanation


def route(complexity: float, load: float) -> RoutingDecision:
    """
    Decide which tier to send this request to.

    Args:
        complexity: classifier score, in [0, 1]
        load: current in-flight / capacity, in [0, 1]

    Returns:
        RoutingDecision — pass `.model` straight to the LLM client.
    """
    # Guard rails — never trust callers.
    complexity = max(0.0, min(1.0, complexity))
    load = max(0.0, min(1.0, load))

    # --- Rule 1: system is nearly saturated. Protect latency at all costs. ---
    if load >= settings.load_critical_threshold:
        return RoutingDecision(
            tier=WEAK,
            model=settings.weak_model,
            reason=(
                f"load {load:.2f} >= critical {settings.load_critical_threshold:.2f}: "
                f"forcing cheap tier to protect p95 latency"
            ),
        )

    # --- Rule 2: complex prompt, but be stricter under high load. ---
    # Under high load, we raise the complexity bar by 0.15 — a "not obviously
    # simple, but not obviously hard" prompt should go to the cheap model
    # while the system is busy.
    effective_bar = settings.complexity_threshold
    if load >= settings.load_high_threshold:
        effective_bar = settings.complexity_threshold + 0.15

    if complexity >= effective_bar:
        return RoutingDecision(
            tier=STRONG,
            model=settings.strong_model,
            reason=(
                f"complexity {complexity:.2f} >= bar {effective_bar:.2f} "
                f"(load {load:.2f}): strong model justified"
            ),
        )

    # --- Rule 3: default is the cheap tier. Most prompts land here. ---
    return RoutingDecision(
        tier=WEAK,
        model=settings.weak_model,
        reason=(
            f"complexity {complexity:.2f} < bar {effective_bar:.2f} "
            f"(load {load:.2f}): cheap tier is sufficient"
        ),
    )
```

---

## `adaptive-rag-router/app/llm_client.py`

```python
"""
Thin wrapper over LiteLLM. Two responsibilities:

  1. Call a given model with a chat-style message list.
  2. Return a normalized result with cost + token usage, so the FastAPI
     handler doesn't have to know LiteLLM internals.

LiteLLM already computes cost from its internal price table and exposes it
at `response._hidden_params["response_cost"]`. If for any reason that field
is missing (e.g., a model LiteLLM doesn't have prices for), we fall back to
computing cost ourselves via `litellm.completion_cost(response)`.
"""
from __future__ import annotations
import asyncio
from dataclasses import dataclass
from typing import Optional

import litellm
from litellm import acompletion, completion_cost

from config import settings


# Silence LiteLLM's verbose logging in the demo.
litellm.suppress_debug_info = True


@dataclass
class LLMResult:
    text: str
    model: str
    cost_usd: float
    prompt_tokens: Optional[int]
    completion_tokens: Optional[int]


def _extract_cost(response) -> float:
    """
    Try the fast path (hidden param), fall back to computing it explicitly.
    Never raises — returns 0.0 if we truly can't figure it out (e.g., local
    Ollama model with no configured price).
    """
    try:
        hp = getattr(response, "_hidden_params", None) or {}
        c = hp.get("response_cost")
        if c is not None:
            return float(c)
    except Exception:
        pass
    try:
        return float(completion_cost(completion_response=response))
    except Exception:
        return 0.0


async def call_model(model: str, prompt: str, rag_context: Optional[str] = None) -> LLMResult:
    """
    Fire one completion call. If rag_context is provided, we prepend a
    system message pinning the model to use only that context.
    """
    messages = []
    if rag_context:
        messages.append({
            "role": "system",
            "content": (
                "Answer the user's question using ONLY the following context. "
                "If the answer isn't in the context, say you don't know.\n\n"
                f"Context:\n{rag_context}"
            ),
        })
    else:
        messages.append({
            "role": "system",
            "content": "You are a concise, helpful assistant.",
        })
    messages.append({"role": "user", "content": prompt})

    response = await acompletion(
        model=model,
        messages=messages,
        max_tokens=settings.max_tokens,
        temperature=settings.temperature,
    )

    text = response.choices[0].message.content or ""
    usage = getattr(response, "usage", None)
    prompt_tokens = getattr(usage, "prompt_tokens", None) if usage else None
    completion_tokens = getattr(usage, "completion_tokens", None) if usage else None

    return LLMResult(
        text=text,
        model=model,
        cost_usd=_extract_cost(response),
        prompt_tokens=prompt_tokens,
        completion_tokens=completion_tokens,
    )
```

---

## `adaptive-rag-router/app/rag.py`

```python
"""
Minimal in-process RAG with Chroma.

- On startup, if the persist directory is empty, we ingest every .txt file
  from data/sample_docs/ into a Chroma collection.
- At query time, we do a top-k similarity search and stitch the results
  into a single context string.

This is deliberately tiny — the RAG isn't the interesting part of the
project, the routing is.
"""
from __future__ import annotations
import os
from pathlib import Path
from typing import List

from langchain_chroma import Chroma
from langchain_core.documents import Document
from langchain_huggingface import HuggingFaceEmbeddings
from langchain_text_splitters import RecursiveCharacterTextSplitter

from config import settings


_COLLECTION = "adaptive_rag_router"


class RagStore:
    def __init__(self):
        self._embeddings = HuggingFaceEmbeddings(model_name=settings.embedding_model)
        self._store = Chroma(
            collection_name=_COLLECTION,
            embedding_function=self._embeddings,
            persist_directory=settings.chroma_persist_dir,
        )

    def is_empty(self) -> bool:
        try:
            return self._store._collection.count() == 0
        except Exception:
            return True

    def ingest_folder(self, folder: str) -> int:
        """Load every .txt file under `folder`, split, embed, store. Returns doc count."""
        p = Path(folder)
        if not p.exists():
            return 0

        raw_docs: List[Document] = []
        for txt in p.rglob("*.txt"):
            content = txt.read_text(encoding="utf-8", errors="ignore")
            raw_docs.append(Document(page_content=content, metadata={"source": str(txt.name)}))

        splitter = RecursiveCharacterTextSplitter(chunk_size=800, chunk_overlap=100)
        chunks = splitter.split_documents(raw_docs)
        if not chunks:
            return 0

        self._store.add_documents(chunks)
        return len(chunks)

    def retrieve(self, query: str, k: int = None) -> str:
        k = k or settings.rag_top_k
        try:
            results = self._store.similarity_search(query, k=k)
        except Exception:
            return ""
        if not results:
            return ""
        return "\n\n---\n\n".join(
            f"[source: {d.metadata.get('source', 'unknown')}]\n{d.page_content}"
            for d in results
        )


# Module-level singleton — Chroma + embeddings are expensive to construct.
_store: RagStore | None = None


def get_store() -> RagStore:
    global _store
    if _store is None:
        _store = RagStore()
        if _store.is_empty():
            n = _store.ingest_folder(settings.sample_docs_dir)
            print(f"[rag] ingested {n} chunks from {settings.sample_docs_dir}")
    return _store
```

---

## `adaptive-rag-router/app/main.py`

```python
"""
FastAPI server wiring everything together.

Two endpoints:
  POST /chat            — adaptive router
  POST /chat_baseline   — always uses the strong model (this is what we
                          compare against in the eval)

Plus a small /stats endpoint that exposes the current load fraction, useful
if you want to eyeball the load tracker live during the demo.
"""
from __future__ import annotations
import asyncio
import time
from contextlib import asynccontextmanager

from fastapi import FastAPI

from app.classifier import score_prompt
from app.llm_client import call_model
from app.load_tracker import LoadTracker
from app.rag import get_store
from app.router import route, STRONG, WEAK
from app.schemas import ChatRequest, ChatResponse
from config import settings


tracker = LoadTracker(capacity=settings.max_concurrent_requests)


@asynccontextmanager
async def lifespan(app: FastAPI):
    # Warm up — construct the embedder and Chroma store once at startup so
    # the very first request isn't a 3-second cold-start outlier in the eval.
    _ = get_store()
    _ = score_prompt("warmup")
    yield


app = FastAPI(title="Adaptive Load-Aware RAG Router", lifespan=lifespan)


@app.get("/health")
async def health():
    return {"ok": True}


@app.get("/stats")
async def stats():
    return {
        "in_flight": tracker.in_flight,
        "capacity": tracker.capacity,
        "load_fraction": tracker.load_fraction,
    }


@app.post("/chat", response_model=ChatResponse)
async def chat(req: ChatRequest):
    """Adaptive router: classifier + load-aware decision."""
    start = time.perf_counter()

    async with tracker.track():
        # 1. Retrieve RAG context (best-effort; empty string if the store is empty).
        #    Chroma.similarity_search + MiniLM embedding are synchronous CPU/IO — run in
        #    a worker thread so we don't freeze the event loop while other requests wait.
        rag_ctx = ""
        if req.use_rag:
            rag_ctx = await asyncio.to_thread(get_store().retrieve, req.prompt)

        # 2. Score complexity (fast, local, no LLM call). Also blocking because of the
        #    embedding call inside — offload for the same reason as (1).
        complexity_result = await asyncio.to_thread(score_prompt, req.prompt)
        complexity = complexity_result.score

        # 3. Read current load (already includes *this* request).
        load = tracker.load_fraction

        # 4. Make the routing decision.
        decision = route(complexity=complexity, load=load)

        # 5. Call the chosen model.
        result = await call_model(decision.model, req.prompt, rag_context=rag_ctx or None)

    latency_ms = (time.perf_counter() - start) * 1000.0

    return ChatResponse(
        answer=result.text,
        tier=decision.tier,
        model=result.model,
        complexity=complexity,
        load_fraction=load,
        routing_reason=decision.reason,
        latency_ms=latency_ms,
        cost_usd=result.cost_usd,
        rag_context_used=bool(rag_ctx),
        prompt_tokens=result.prompt_tokens,
        completion_tokens=result.completion_tokens,
    )


@app.post("/chat_baseline", response_model=ChatResponse)
async def chat_baseline(req: ChatRequest):
    """
    Naive baseline: always use the strong model.
    Same load tracker, same RAG, same everything — the ONLY difference is
    that routing is disabled and we go straight to the expensive tier.
    This is what the eval compares against.
    """
    start = time.perf_counter()

    async with tracker.track():
        rag_ctx = ""
        if req.use_rag:
            rag_ctx = await asyncio.to_thread(get_store().retrieve, req.prompt)

        # Still scored for reporting; offload for the same reason as /chat.
        complexity_result = await asyncio.to_thread(score_prompt, req.prompt)
        complexity = complexity_result.score
        load = tracker.load_fraction
        result = await call_model(settings.strong_model, req.prompt, rag_context=rag_ctx or None)

    latency_ms = (time.perf_counter() - start) * 1000.0
    return ChatResponse(
        answer=result.text,
        tier="baseline_strong",
        model=result.model,
        complexity=complexity,
        load_fraction=load,
        routing_reason="baseline: always strong",
        latency_ms=latency_ms,
        cost_usd=result.cost_usd,
        rag_context_used=bool(rag_ctx),
        prompt_tokens=result.prompt_tokens,
        completion_tokens=result.completion_tokens,
    )
```

---

## `adaptive-rag-router/data/sample_docs/company_faq.txt`

```txt
FROBOZZ TECHNICAL SERVICES — INTERNAL FAQ

Q: What are our support hours?
A: 24/7 for P1 incidents. P2 and P3 tickets are handled 09:00–18:00 UTC
   Monday through Friday. On-call rotation is documented in the runbook.

Q: How do employees request time off?
A: Submit a leave request via the HR portal at least two weeks in advance.
   For urgent leave, contact your manager directly and file the request
   retroactively within 48 hours.

Q: What is the expense reimbursement policy?
A: Employees may expense up to USD 75 per meal while traveling on company
   business. Anything above USD 200 requires manager pre-approval. Receipts
   must be uploaded within 14 days.

Q: What is our security incident escalation policy?
A: Any suspected breach must be reported to the on-call security lead
   within 30 minutes of discovery. A P1 incident ticket must be opened
   immediately. Do not remediate before security has captured forensic data.
```

---

## `adaptive-rag-router/data/sample_docs/product_notes.txt`

```txt
FROBOZZ QUANTUM WIDGET — RELEASE NOTES

v3.2.0 (released 2026-02-14)
- Added support for the new Photon Lattice API.
- Reduced cold-start latency by 42% via lazy initialization of the
  entanglement cache.
- Deprecated the legacy /widget/v1 endpoint; it will be removed in v4.0.

v3.1.0 (released 2025-11-20)
- Introduced tiered pricing: Bronze (up to 10k widgets/mo),
  Silver (up to 100k), and Gold (unlimited).
- Fixed a bug where widgets with zero mass would loop indefinitely
  during initialization.

Known issues
- The Photon Lattice API returns HTTP 429 more aggressively than
  documented. Recommend exponential backoff starting at 250 ms.
- Firefox 141 users may see a flicker during widget hydration; a fix
  is planned for v3.2.1.
```

---

## `adaptive-rag-router/data/sample_docs/engineering_glossary.txt`

```txt
ENGINEERING GLOSSARY

Adaptive Router
   A system component that decides at request time which downstream model
   or service should handle a given request, typically based on cost,
   latency, and quality trade-offs.

Cold Start
   The additional latency incurred when a service or model is invoked
   after a period of inactivity, due to initialization overhead
   (loading weights, warming caches, spinning up containers).

Load Fraction
   The ratio of in-flight requests to the maximum concurrent capacity of
   a service. A load fraction of 1.0 means the service is fully saturated.

p95 Latency
   The 95th percentile of a latency distribution: 95% of requests
   complete faster than this value. A common tail-latency SLO metric.

Retrieval-Augmented Generation (RAG)
   A pattern where an LLM's answer is grounded in documents retrieved
   from an external knowledge base, typically via vector similarity
   search over embedded text chunks.
```

---

## `adaptive-rag-router/eval/labeled_prompts.json`

```json
[
  {"prompt": "Hi, who are you?", "expected_tier": "weak"},
  {"prompt": "What are our support hours?", "expected_tier": "weak"},
  {"prompt": "What is p95 latency?", "expected_tier": "weak"},
  {"prompt": "Summarize the release notes for v3.2.0.", "expected_tier": "weak"},
  {"prompt": "Who do I contact for time off requests?", "expected_tier": "weak"},
  {"prompt": "What is the max meal expense?", "expected_tier": "weak"},
  {"prompt": "Define cold start.", "expected_tier": "weak"},
  {"prompt": "Is 09:00 UTC within support hours?", "expected_tier": "weak"},
  {"prompt": "List the known issues for the Quantum Widget.", "expected_tier": "weak"},

  {"prompt": "Compare the trade-offs between exponential backoff and token-bucket rate limiting when calling an upstream API that returns HTTP 429 aggressively. Explain when each is preferable and why, step by step.", "expected_tier": "strong"},
  {"prompt": "Design an in-memory concurrency-aware router that decides between two LLM tiers based on prompt complexity and load. Explain the trade-offs of using a semaphore versus a token bucket, and derive the p95 latency impact of misrouting a fraction f of requests to the slow tier under 50 concurrent users.", "expected_tier": "strong"},
  {"prompt": "Given a Chroma vector store with 1M embeddings and top-k=10 retrieval, analyze how HNSW parameter choices (M, efConstruction, efSearch) trade recall against query latency. Propose a tuning strategy for a service with a p95 latency budget of 80 ms.", "expected_tier": "strong"},
  {"prompt": "Prove that a weighted-round-robin scheduler with weights (w1, w2) is equivalent, in the long run, to independently sampling from Bernoulli(w1 / (w1+w2)). Derive the variance of the imbalance over N requests.", "expected_tier": "strong"},
  {"prompt": "Refactor the following Python function for readability and to reduce its cyclomatic complexity, and also explain each change:\\n```python\\ndef f(x, y, z):\\n    if x:\\n        if y:\\n            if z:\\n                return 1\\n            else:\\n                return 2\\n        else:\\n            return 3\\n    else:\\n        return 4\\n```", "expected_tier": "strong"},
  {"prompt": "Explain the difference between BM25 and dense retrieval, when a hybrid approach helps, and how you would evaluate the hybrid on a private FAQ corpus. Include a proposed metric and a small experiment design.", "expected_tier": "strong"},
  {"prompt": "Derive the expected cost savings per 1000 requests for a two-tier router that sends fraction p of traffic to a cheap model at $0.10 / MTok and (1-p) to an expensive model at $3.00 / MTok, assuming an average of 500 tokens per response. Then compute p such that the expected cost is 20% of the always-expensive baseline.", "expected_tier": "strong"},

  {"prompt": "Explain step by step how to reset my password.", "expected_tier": "strong"},
  {"prompt": "What?", "expected_tier": "weak"}
]
```

---

## `adaptive-rag-router/eval/load_test.py`

```python
"""
Load simulator.

Fires N concurrent requests at BOTH endpoints (/chat and /chat_baseline)
using the labeled prompt set (recycled if N > len(prompts)), measures
latency and cost, and writes results to results/<endpoint>_<N>.json.

Run:
    python -m eval.load_test --levels 5 15 30 50 --url http://localhost:8000

Then plot with:
    python -m eval.plot_results
"""
from __future__ import annotations
import argparse
import asyncio
import json
import os
import statistics
import time
from pathlib import Path

import httpx
from tqdm.asyncio import tqdm_asyncio


HERE = Path(__file__).resolve().parent
PROMPTS_FILE = HERE / "labeled_prompts.json"
RESULTS_DIR = HERE / "results"
RESULTS_DIR.mkdir(exist_ok=True)


def load_prompts():
    with PROMPTS_FILE.open("r", encoding="utf-8") as f:
        return json.load(f)


async def _one(client: httpx.AsyncClient, url: str, prompt: str):
    t0 = time.perf_counter()
    try:
        r = await client.post(url, json={"prompt": prompt, "use_rag": True}, timeout=120.0)
        r.raise_for_status()
        body = r.json()
        return {
            "ok": True,
            "latency_ms": (time.perf_counter() - t0) * 1000.0,
            "server_latency_ms": body.get("latency_ms"),
            "tier": body.get("tier"),
            "model": body.get("model"),
            "cost_usd": body.get("cost_usd", 0.0),
            "complexity": body.get("complexity"),
            "load_fraction": body.get("load_fraction"),
            "prompt_tokens": body.get("prompt_tokens"),
            "completion_tokens": body.get("completion_tokens"),
        }
    except Exception as e:
        return {
            "ok": False,
            "error": str(e),
            "latency_ms": (time.perf_counter() - t0) * 1000.0,
        }


async def run_level(base_url: str, endpoint: str, n_concurrent: int, prompts):
    url = f"{base_url}{endpoint}"
    # Cycle prompts if we need more than the labeled set.
    tasks_prompts = [prompts[i % len(prompts)]["prompt"] for i in range(n_concurrent)]

    async with httpx.AsyncClient() as client:
        coros = [_one(client, url, p) for p in tasks_prompts]
        results = await tqdm_asyncio.gather(
            *coros, desc=f"{endpoint} N={n_concurrent}"
        )

    ok = [r for r in results if r["ok"]]
    lats = [r["latency_ms"] for r in ok]
    costs = [r["cost_usd"] or 0.0 for r in ok]
    tier_counts = {}
    for r in ok:
        tier_counts[r["tier"]] = tier_counts.get(r["tier"], 0) + 1

    summary = {
        "endpoint": endpoint,
        "n_concurrent": n_concurrent,
        "n_ok": len(ok),
        "n_err": len(results) - len(ok),
        "latency_ms": {
            "avg": statistics.mean(lats) if lats else 0.0,
            "p50": statistics.median(lats) if lats else 0.0,
            "p95": (statistics.quantiles(lats, n=20)[18] if len(lats) >= 20
                    else (max(lats) if lats else 0.0)),
            "max": max(lats) if lats else 0.0,
        },
        "total_cost_usd": sum(costs),
        "avg_cost_usd": (sum(costs) / len(ok)) if ok else 0.0,
        "tier_counts": tier_counts,
        "raw": results,
    }

    tag = endpoint.strip("/").replace("/", "_") or "root"
    out = RESULTS_DIR / f"{tag}_N{n_concurrent}.json"
    with out.open("w", encoding="utf-8") as f:
        json.dump(summary, f, indent=2)
    print(f"[saved] {out}")
    return summary


async def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--url", default="http://localhost:8000")
    ap.add_argument("--levels", type=int, nargs="+", default=[5, 15, 30, 50])
    ap.add_argument("--endpoints", nargs="+", default=["/chat", "/chat_baseline"])
    args = ap.parse_args()

    prompts = load_prompts()

    # Sanity: does the server respond?
    async with httpx.AsyncClient() as client:
        try:
            r = await client.get(f"{args.url}/health", timeout=10.0)
            r.raise_for_status()
        except Exception as e:
            print(f"[fatal] server not reachable at {args.url}: {e}")
            return

    for n in args.levels:
        for ep in args.endpoints:
            await run_level(args.url, ep, n, prompts)
            # Small breather so tiers can drain between runs.
            await asyncio.sleep(1.0)


if __name__ == "__main__":
    asyncio.run(main())
```

---

## `adaptive-rag-router/eval/plot_results.py`

```python
"""
Reads eval/results/*.json, produces:

  - eval/results/latency_comparison.png
  - eval/results/cost_comparison.png
  - eval/results/tier_distribution.png
  - eval/results/routing_accuracy.png
  - eval/results/summary.md   (markdown table + percentages)

Run:
    python -m eval.plot_results
"""
from __future__ import annotations
import json
import re
from pathlib import Path
from typing import Dict, List

import httpx
import matplotlib.pyplot as plt

HERE = Path(__file__).resolve().parent
RESULTS_DIR = HERE / "results"
PROMPTS_FILE = HERE / "labeled_prompts.json"


# ---- Load result JSONs -------------------------------------------------

def load_summaries() -> Dict[str, Dict[int, dict]]:
    """Returns {endpoint: {N: summary}} for both /chat and /chat_baseline."""
    out: Dict[str, Dict[int, dict]] = {"chat": {}, "chat_baseline": {}}
    for p in RESULTS_DIR.glob("*.json"):
        m = re.match(r"(chat|chat_baseline)_N(\d+)\.json", p.name)
        if not m:
            continue
        ep, n = m.group(1), int(m.group(2))
        with p.open("r", encoding="utf-8") as f:
            out[ep][n] = json.load(f)
    return out


def _pct_drop(new: float, base: float) -> float:
    if base <= 0:
        return 0.0
    return (base - new) / base * 100.0


# ---- Charts -----------------------------------------------------------

def plot_latency(summaries):
    levels = sorted(set(summaries["chat"].keys()) & set(summaries["chat_baseline"].keys()))
    fig, ax = plt.subplots(figsize=(9, 5))

    for metric, marker in [("avg", "o"), ("p50", "s"), ("p95", "^")]:
        adaptive = [summaries["chat"][n]["latency_ms"][metric] for n in levels]
        baseline = [summaries["chat_baseline"][n]["latency_ms"][metric] for n in levels]
        ax.plot(levels, adaptive, marker=marker, label=f"adaptive {metric}")
        ax.plot(levels, baseline, marker=marker, linestyle="--", label=f"baseline {metric}")

    ax.set_xlabel("Concurrent users")
    ax.set_ylabel("Latency (ms)")
    ax.set_title("Latency vs concurrency: adaptive router vs always-strong baseline")
    ax.grid(True, alpha=0.3)
    ax.legend()
    out = RESULTS_DIR / "latency_comparison.png"
    fig.tight_layout()
    fig.savefig(out, dpi=140)
    plt.close(fig)
    print(f"[saved] {out}")


def plot_cost(summaries):
    levels = sorted(set(summaries["chat"].keys()) & set(summaries["chat_baseline"].keys()))
    fig, ax = plt.subplots(figsize=(9, 5))

    x = range(len(levels))
    width = 0.35
    adaptive = [summaries["chat"][n]["total_cost_usd"] for n in levels]
    baseline = [summaries["chat_baseline"][n]["total_cost_usd"] for n in levels]
    ax.bar([i - width / 2 for i in x], adaptive, width, label="adaptive")
    ax.bar([i + width / 2 for i in x], baseline, width, label="baseline (always strong)")

    ax.set_xticks(list(x))
    ax.set_xticklabels([str(n) for n in levels])
    ax.set_xlabel("Concurrent users")
    ax.set_ylabel("Total USD cost for the batch")
    ax.set_title("Cost vs concurrency")
    ax.legend()
    ax.grid(True, alpha=0.3, axis="y")

    for i, (a, b) in enumerate(zip(adaptive, baseline)):
        drop = _pct_drop(a, b)
        ax.text(i, max(a, b) * 1.02, f"-{drop:.0f}%", ha="center", fontsize=9)

    out = RESULTS_DIR / "cost_comparison.png"
    fig.tight_layout()
    fig.savefig(out, dpi=140)
    plt.close(fig)
    print(f"[saved] {out}")


def plot_tier_distribution(summaries):
    levels = sorted(summaries["chat"].keys())
    fig, ax = plt.subplots(figsize=(9, 5))

    weak = [summaries["chat"][n]["tier_counts"].get("weak", 0) for n in levels]
    strong = [summaries["chat"][n]["tier_counts"].get("strong", 0) for n in levels]

    x = range(len(levels))
    ax.bar(x, weak, label="weak (cheap)")
    ax.bar(x, strong, bottom=weak, label="strong (expensive)")
    ax.set_xticks(list(x))
    ax.set_xticklabels([str(n) for n in levels])
    ax.set_xlabel("Concurrent users")
    ax.set_ylabel("Requests routed")
    ax.set_title("Adaptive router — tier distribution vs load")
    ax.legend()
    ax.grid(True, alpha=0.3, axis="y")

    out = RESULTS_DIR / "tier_distribution.png"
    fig.tight_layout()
    fig.savefig(out, dpi=140)
    plt.close(fig)
    print(f"[saved] {out}")


# ---- Routing accuracy on hand-labeled prompts ------------------------

def measure_routing_accuracy(base_url: str = "http://localhost:8000") -> dict:
    """
    Sends each labeled prompt one-at-a-time (so load ≈ 0 and we get pure
    complexity-driven decisions) and compares router output to labels.
    """
    with PROMPTS_FILE.open("r", encoding="utf-8") as f:
        prompts = json.load(f)

    correct = 0
    rows = []
    with httpx.Client(timeout=120.0) as client:
        for item in prompts:
            try:
                r = client.post(
                    f"{base_url}/chat",
                    json={"prompt": item["prompt"], "use_rag": True},
                )
                r.raise_for_status()
                body = r.json()
                got = body["tier"]
            except Exception as e:
                got = f"ERROR: {e}"
            ok = got == item["expected_tier"]
            correct += int(ok)
            rows.append({
                "prompt": item["prompt"][:80] + ("…" if len(item["prompt"]) > 80 else ""),
                "expected": item["expected_tier"],
                "got": got,
                "ok": ok,
            })

    acc = correct / len(prompts) if prompts else 0.0
    return {"accuracy": acc, "correct": correct, "total": len(prompts), "rows": rows}


def plot_routing_accuracy(acc: dict):
    fig, ax = plt.subplots(figsize=(6, 4))
    ax.bar(["correct", "incorrect"], [acc["correct"], acc["total"] - acc["correct"]])
    ax.set_title(
        f"Routing accuracy on hand-labeled set: "
        f"{acc['correct']}/{acc['total']} = {acc['accuracy']*100:.1f}%"
    )
    ax.grid(True, alpha=0.3, axis="y")
    out = RESULTS_DIR / "routing_accuracy.png"
    fig.tight_layout()
    fig.savefig(out, dpi=140)
    plt.close(fig)
    print(f"[saved] {out}")


# ---- Markdown summary ------------------------------------------------

def write_markdown_summary(summaries, acc):
    levels = sorted(set(summaries["chat"].keys()) & set(summaries["chat_baseline"].keys()))
    lines: List[str] = []
    lines.append("# Adaptive Router — Eval Summary\n")
    lines.append("## Latency and cost vs concurrency\n")
    lines.append("| N | avg (adapt) | avg (base) | p95 (adapt) | p95 (base) | "
                 "cost (adapt) | cost (base) | latency Δ | cost Δ |")
    lines.append("|---:|---:|---:|---:|---:|---:|---:|---:|---:|")
    for n in levels:
        a = summaries["chat"][n]
        b = summaries["chat_baseline"][n]
        lat_delta = _pct_drop(a["latency_ms"]["avg"], b["latency_ms"]["avg"])
        cost_delta = _pct_drop(a["total_cost_usd"], b["total_cost_usd"])
        lines.append(
            f"| {n} "
            f"| {a['latency_ms']['avg']:.0f} ms | {b['latency_ms']['avg']:.0f} ms "
            f"| {a['latency_ms']['p95']:.0f} ms | {b['latency_ms']['p95']:.0f} ms "
            f"| ${a['total_cost_usd']:.4f} | ${b['total_cost_usd']:.4f} "
            f"| **-{lat_delta:.0f}%** | **-{cost_delta:.0f}%** |"
        )

    lines.append("\n## Tier distribution (adaptive)\n")
    lines.append("| N | weak (cheap) | strong (expensive) |")
    lines.append("|---:|---:|---:|")
    for n in levels:
        tc = summaries["chat"][n]["tier_counts"]
        lines.append(f"| {n} | {tc.get('weak', 0)} | {tc.get('strong', 0)} |")

    lines.append("\n## Routing accuracy on hand-labeled prompts\n")
    lines.append(
        f"**{acc['correct']} / {acc['total']} = {acc['accuracy']*100:.1f}%**\n"
    )
    lines.append("| Expected | Got | Prompt |")
    lines.append("|---|---|---|")
    for r in acc["rows"]:
        mark = "✅" if r["ok"] else "❌"
        lines.append(f"| {r['expected']} | {r['got']} {mark} | {r['prompt']} |")

    out = RESULTS_DIR / "summary.md"
    with out.open("w", encoding="utf-8") as f:
        f.write("\n".join(lines))
    print(f"[saved] {out}")


# ---- Entrypoint -------------------------------------------------------

def main():
    summaries = load_summaries()
    if not summaries["chat"] or not summaries["chat_baseline"]:
        print("[warn] no result files yet — run `python -m eval.load_test` first.")
        return
    plot_latency(summaries)
    plot_cost(summaries)
    plot_tier_distribution(summaries)

    print("[info] measuring routing accuracy on labeled prompts (one at a time)…")
    acc = measure_routing_accuracy()
    plot_routing_accuracy(acc)

    write_markdown_summary(summaries, acc)
    print("\nDone. Open eval/results/summary.md and the .png files.")


if __name__ == "__main__":
    main()
```

---

## `adaptive-rag-router/README.md`

```markdown
# Adaptive, Load-Aware RAG Router

A single-process FastAPI service that answers questions with RAG and
routes each request to a cheap or an expensive LLM based on prompt
complexity AND current in-process concurrency load.

See the top of the parent MD file for detailed run instructions.
Short version:

1. `python -m venv .venv && .venv\Scripts\activate` (or `source .venv/bin/activate` on Unix)
2. `pip install -r requirements.txt`
3. Copy `.env.example` → `.env`, fill in API keys.
4. `uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 1`
5. `python -m eval.load_test --levels 5 15 30 50`
6. `python -m eval.plot_results`
7. Open `eval/results/summary.md` and the PNGs.
```

---

# Full run instructions (do these yourself — I do NOT install or download anything)

## Step 1 — Create the folder and files

Create `C:\Users\arpant\adaptive-rag-router\` (per your memory, avoid OneDrive) and paste each file above into its exact path. Create empty subfolders `app/`, `data/sample_docs/`, `eval/`, and `eval/results/` if needed.

## Step 2 — Python environment

Open PowerShell in `C:\Users\arpant\adaptive-rag-router\` and run:

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
pip install -r requirements.txt
```

If your Python is under 3.10, upgrade first — LangChain 0.3 requires 3.10+.

## Step 3 — Get API keys

You need **one** strong-model key AND the Groq key.

- **Anthropic** (recommended strong tier): sign up at https://console.anthropic.com/ → API Keys → create → paste into `.env` as `ANTHROPIC_API_KEY=…`.
- **OR OpenAI** (alternative strong tier): https://platform.openai.com/api-keys. If you use this instead, edit `.env` to comment the Anthropic lines and uncomment the OpenAI lines.
- **Groq** (cheap tier, free tier is plenty): https://console.groq.com/keys → paste into `.env` as `GROQ_API_KEY=…`.

Copy `.env.example` to `.env` and fill it in.

## Step 4 — The embedding model download

This is the only model you need to download. The first time you start the server, `sentence-transformers` will auto-download `all-MiniLM-L6-v2` (~90 MB) from Hugging Face to `~/.cache/huggingface/`.

If you want to download it manually first (recommended so the first request in the eval isn't slow):

- **Hugging Face model page:** https://huggingface.co/sentence-transformers/all-MiniLM-L6-v2

You can pre-warm it with a one-liner in Python:

```python
from langchain_huggingface import HuggingFaceEmbeddings
HuggingFaceEmbeddings(model_name="sentence-transformers/all-MiniLM-L6-v2")
```

## Step 5 — (Optional) local model via Ollama

You do NOT need this to run the demo. Only do this if you want a fully-offline demo. If you want it:

- Install Ollama from https://ollama.com/download/windows
- Pull the model: run in a terminal (**you** run this, not me):
  ```
  ollama pull llama3.2:3b
  ```
- **Ollama model page:** https://ollama.com/library/llama3.2
- In `.env`, set `WEAK_MODEL=ollama/llama3.2:3b` instead of the Groq one, and unset `GROQ_API_KEY`.

## Step 6 — Start the server

In the project folder with the venv active:

```powershell
uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 1
```

**Important:** `--workers 1`. This is a single-process demo — multiple workers would fragment the load counter and break the whole premise.

On startup you should see something like:

```
[rag] ingested 8 chunks from ./data/sample_docs
INFO:     Uvicorn running on http://0.0.0.0:8000
```

Try a smoke test in another terminal:

```powershell
curl -X POST http://localhost:8000/chat -H "Content-Type: application/json" -d "{\"prompt\": \"What are our support hours?\"}"
```

You should see JSON with `"tier": "weak"` and a very small `cost_usd`.

## Step 7 — Run the load test

Leave the server running. In a **second** PowerShell (activate the venv there too):

```powershell
python -m eval.load_test --levels 5 15 30 50
```

This fires N concurrent requests at both `/chat` and `/chat_baseline` for each N in `{5, 15, 30, 50}`, and writes eight JSON files to `eval/results/`. Expect this to take a couple of minutes.

## Step 8 — Generate charts and the summary

```powershell
python -m eval.plot_results
```

This produces:

- `eval/results/latency_comparison.png` — avg/p50/p95 latency vs concurrency for both endpoints
- `eval/results/cost_comparison.png` — total USD spent for the batch at each concurrency level, with the % savings annotated on top
- `eval/results/tier_distribution.png` — how often the router chose cheap vs expensive at each concurrency
- `eval/results/routing_accuracy.png` — accuracy against your 18 hand-labeled prompts
- `eval/results/summary.md` — a markdown table with everything above, ready to screenshot or paste into your CV / project README / talking points

## Step 9 — What you'll say in the interview

Point at the four PNGs and the summary. The talking points that fall out of this project naturally:

- **"Cost:** at 50 concurrent users the adaptive router spent $X vs the always-strong baseline's $Y — an N% reduction, because ~M% of prompts fell below the complexity bar and went to the cheap tier."
- **"Latency:** p95 was N% lower under load because we forced the cheap tier once load fraction crossed 0.9, avoiding the strong model's queueing tail."
- **"Correctness:** on the 18-prompt hand-labeled set the router matched my labels N/18 = M% of the time — the misses (if any) are all borderline prompts I'd argue about myself."
- **"Design:** the routing decision is one 30-line function in `app/router.py`, with two normalized inputs and three explicit rules. That's on purpose — a learned router would be faster to justify to a research audience but harder to justify to an SRE."

## Step 10 — If something breaks

- **`RateLimitError` from Groq / Anthropic:** you hit the free-tier rate limit. Wait a minute, or lower `--levels`.
- **Chroma errors on first run:** delete `chroma_db/` and restart the server.
- **Very slow first request:** the embedder is downloading MiniLM. Wait once, subsequent runs use the local cache.
- **Empty RAG context:** confirm `data/sample_docs/` has the three .txt files. The `[rag] ingested N chunks` line at startup tells you if ingestion ran.
- **`ModuleNotFoundError: config`:** you're running from the wrong folder. All commands should be run from `adaptive-rag-router/`, not from inside `app/` or `eval/`.

That's the whole project. Nothing else to do on my side — everything from here (installs, downloads, execution) is yours.
